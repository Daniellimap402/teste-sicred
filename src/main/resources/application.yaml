server:
    port:
        5000
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/sicred
        username: postgres
        password: postgres

    jpa:
        show-sql: true
        generate-ddl: true
        hibernate:
            ddl-auto: create